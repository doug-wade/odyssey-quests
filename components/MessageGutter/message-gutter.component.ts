import { defineComponent, html } from "@tybalt/core";

export default defineComponent({
    name: 'oq-message-gutter',
    render() {
        return html`
            <p>message gutter</p>
        `;
    }
});