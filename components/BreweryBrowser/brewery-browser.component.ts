import { defineComponent, html } from "@tybalt/core";

export default defineComponent({
    name: 'oq-brewery-browser',
    render() {
        return html`
            <p>brewery browser</p>
        `;
    }
});